import { AuthenticationComponent } from '../../component/authentication/AuthenticationComponent';

export const AppHeader = () => {
    return (
        <header className="App-header">
            <h1>Data Explore</h1>
            <AuthenticationComponent>
                
            </AuthenticationComponent>
        </header>
    );
  };
  